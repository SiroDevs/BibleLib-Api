# MongoDB connection string
ATLAS_URI=mongodb://localhost:27017/biblelib

# Port the server listens on (defaults to 4000)
PORT=4000

# Required for all write operations (POST, PUT, DELETE) via the x-api-key header
API_KEY=change-me

# Comma-separated list of allowed CORS origins, or * for all
ALLOWED_ORIGINS=*
